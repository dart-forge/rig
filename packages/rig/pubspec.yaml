name: rig
description: Your tests start the containers they need. Declare a container, get a ready port.
version: 0.4.0
repository: https://github.com/dart-forge/rig
topics:
  - testing
  - docker
  - containers
  - integration-testing

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  crypto: ^3.0.0
  meta: ^1.15.0
  path: ^1.9.0
  test: ^1.25.0

dev_dependencies:
  lints: ^5.0.0
