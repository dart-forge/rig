name: rig_redis
description: Redis containers for Dart tests — a password that is genuinely enforced, and no client dependency.
version: 0.1.0
repository: https://github.com/dart-forge/rig
topics:
  - testing
  - docker
  - redis
  - integration-testing

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  path: ^1.9.0
  rig: ^0.1.0
  test: ^1.25.0

dev_dependencies:
  lints: ^5.0.0
