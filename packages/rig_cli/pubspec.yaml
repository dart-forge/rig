name: rig_cli
description: Command line tool for rig — list and clean up the containers it keeps.
version: 0.0.1
publish_to: none
repository: https://github.com/dart-forge/rig

environment:
  sdk: ^3.13.0

resolution: workspace

executables:
  rig:

dependencies:
  args: ^2.5.0
  path: ^1.9.0
  rig: ^0.0.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
